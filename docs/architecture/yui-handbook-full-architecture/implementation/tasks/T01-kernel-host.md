# T01｜基础宿主与操作事实

硬依赖：T00。

## 交付目标

在现有持久化和授权基础上建立一个实例宿主，并明确外部操作的请求、处置和回执。

**完成状态：** 插件或适配实现可注册、取得句柄和释放；本地事实及外部操作仍共享现有权威存储。

## 责任范围

- 建立实例身份、effect disposer、短调用引用和长期 Session 引用。
- 对外部动作使用稳定请求身份，保留 unknown 与部分成功。
- 把认证后的调用上下文传给实现，不信任插件输入中的角色。

## 可独立分工的工作

1. 实现宿主和确定性 effect 测试。
2. 复用操作记录并验证去重与 unknown。
3. 验证现有存储兼容和授权入口。

这些分工供 Leader 判断，不要求按步骤建立 WorkItem。紧密耦合的实现应由同一责任单元完成。

## 不在范围内

- 不做全局插件市场、跨进程服务发现或分布式租约。
- 不创建 Operator 自动恢复机制。

## 完成证据

- 失败与外部效果可以同时表示，重复请求不盲目重发。
- 宿主卸载只回收其拥有的资源，仍被使用的句柄不提前销毁。
- 正常本地查询不增加一层操作账本。

验收场景：S09、S10、S20、S25、S34、S44、S45。场景定义见[统一验收说明](../../reference/acceptance-scenarios.md)。

## 实施入口

[详细技术方案](../designs/T01-kernel-host.md)定义接口、改动位置的定位方法、存储影响、兼容路径和具体验证。共同执行约束见[实施路线](../README.md)。
