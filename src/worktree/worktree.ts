export type RoleWorktree = {
  schemaVersion: 2;
  taskId: string;
  roleName: string;
  repositoryRoot: string;
  commonDir: string;
  repositoryFingerprint: string;
  path: string;
  worktreeGitDir: string;
  branchRef: string;
  headOid: string;
  ownerToken: string;
  createdAt: string;
};
